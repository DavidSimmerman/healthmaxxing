# Findings triage (untracked scratch). Status: [ ]=todo [x]=fixed [-]=won't fix (reason)

## Mechanical

- [ ] M1 lint: prettier drift 36 files (fixed by format run, commit pending)
- [ ] M2 eslint: 28x no-navigation-without-resolve (no base path ever → disable rule),
  10x no-explicit-any (→ warn), 8x prefer-svelte-reactivity (build-and-goto URLSearchParams
  false positive — per-file disable), 1x require-each-key (CHECK — may be real),
  1x no-unused-svelte-ignore, 1x no-unused-props, 1x no-at-html-tags (reports/[id]:43 —
  sanitized server-side w/ allowlist, VERIFIED ok → inline disable), 1x unused import `and`
  (day/[date]/+page.server.ts:2)
- [ ] M3 svelte.config.js uses deprecated kit.csrf.checkOrigin → csrf.trustedOrigins (check what it's set to!)

## Reviewer 2 — domain logic (all verified by reviewer w/ quotes; re-verify at edit time)

- [ ] F2.1 DATA foods.ts:18 posNum() accepts negatives → recipe macros can go negative into DB
  and silently zero in netCarbs bolus calc. FIX: clamp v>=0 (match nonNeg/sanitizeNutrients).
- [ ] F2.2 P2 projections.ts:170 `-windowDays + 1` vs :459 `-windowDays` — bodyInsights window
  is 1 day short vs energyInsights (author fixed only one site, comment at :456 proves intent).
  FIX: drop the +1 at :170.
- [ ] F2.3 PERF deficit.ts:81 per-day `[...compByDate].reverse().find()` = O(days×rows), and
  bodyComp query has no lower date bound (fetches all-time). FIX: forward pointer + gte bound.
- [ ] F2.4 DATA api/planned/+server.ts:24-27 schedule endpoint missing future-time check that
  MCP twin (foods.ts parseScheduleAt) enforces. FIX: reuse parseScheduleAt. (HOLD until api
  reviewer returns — same file may get more findings.)
- [ ] F2.5 PERF energy.ts:98 interpolateGaps same reverse+find pattern. FIX: forward pointer.

## Reviewer 3 — integrations

- [ ] F3.1 P1 dexcom.ts:102-112 refresh rotation: tokenRequest consumed old token; if
  storeRefresh throws, new token lost forever. FIX: retry w/ backoff around storeRefresh.
- [ ] F3.2 P1 fitbit.ts:144-148 getJson returns null on ANY !ok (401/403/429/5xx look like
  "unsupported type") → sync reports success forever on dead connection. FIX: throw on
  auth/rate/server errors, null only on benign 4xx.
- [ ] F3.3 P2 dexcom.ts:168-180 no dedupe before batched onConflictDoUpdate (tandem.ts:188-208
  has the guard + comment). Same gap: workouts route (hkUuid), metrics route ((date,metric)).
  FIX: Map-dedupe in all three. (routes: HOLD for api reviewer)
- [ ] F3.4 P2 no fetch timeouts: fitbit.ts:59,145,209 dexcom.ts:63,151,228 openFoodFacts.ts:97,193.
  FIX: AbortSignal.timeout(10s) mirroring fdc.ts:61.
- [ ] F3.5 P2 dexcom refresh on EVERY sync + no lock → invited race (sync route allows session+cron).
  FIX: in-process cache {accessToken, expiresAt} from expires_in; refresh only when expired.
  Do same for fitbit if shape matches.
- [ ] F3.6 DATA fitbit.ts:116-143 sleep always pageSize 25 ignoring days window (others honor it).
  FIX: read file; honor days if endpoint allows, else surface effective window.
- [ ] F3.7 NIT fitbit syncHealth/peekHealth missing clampDays guard its siblings have.
- [ ] E3.1 PERF barcode/[code] route: findFoodByBarcode + lookupBarcode serial → Promise.all. (HOLD routes)
- [ ] E3.3 PERF dexcom.ts:190-219 rollUpDays loads every reading into Node → GROUP BY in SQL.

## Cross-cutting efficiency (reviewer 2)
- [ ] E2.1 day-bucket SQL fragment `(col at time zone 'UTC' at time zone APP_TZ)::date`
  duplicated across deficit.ts, nutrition.ts x2, healthMetrics.ts, goals.ts → one helper in day.ts.

## Feature ideas pool (for morning report)
- Allulose: netCarbs.ts implements+tests alluloseG subtraction but no Nutrients key / call site passes it — unreachable. Finish wiring.
- Integration health surface in /settings (updatedAt columns exist; findings F3.2/F3.5 = silent death today).
- Persist last-sync counts per source → alertable "0 new for N days".
- sleepInsights halfSplitDelta primitive → GMI/TIR trend card reusing tested code.

## Still waiting on reviewers: 1=db 4=auth/security 5=api-correctness 6=UI 7=sidecar/deploy
